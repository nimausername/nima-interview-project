import { PagePhotos } from './PagePhotos'

xdescribe(PagePhotos.name, () => {
	// TODO [Task 4]: **Only outline** the test cases without implementing them.

	it('should do A when B happens given C', () => { })
})